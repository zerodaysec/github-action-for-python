on: push
name: on push
jobs:
  gitHubActionForPylint:
    name: Python linting
    runs-on: ubuntu-latest
    steps:

    - uses: actions/checkout@master

    # - name: GitHub Action for pylint
    #   uses: zerodaysec/github-action-for-python@master
    #   with:
    #     args: pylint --help

    # - name: GitHub Action for pylint
    #   uses: zerodaysec/github-action-for-python@master
    #   with:
    #     args: pylint --help
